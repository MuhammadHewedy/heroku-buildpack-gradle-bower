#!/usr/bin/env bash
# bin/use <build-dir>


if [[ -f $1/build.gradle && -f $1/src/main/resources/static/package.json ]]; then
  echo "Gradle/SpringBoot/Angular" && exit 0
else
  echo "no" && exit 1
fi
